import React from 'react';

const PromotionDetails = ({ match, location }) => {
    const { params: { id } } = match;
    return (
      <>
        TODO: Looking for promotion {id} in the back-end.
      </>
    );
  };

export default PromotionDetails;